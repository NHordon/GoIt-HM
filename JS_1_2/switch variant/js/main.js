
var arrName = [];

	for(var i = 0; i < 5; i++) {
	arrName[i] = prompt('Заполняем массив имен, имя №'+(i+1)+":");
}

alert("Итого мы имеем массив: " + arrName);
	
var name = prompt ('Введите имя');
switch(name) {
	case ( arrName[i]):
	alert ( arrName[i] + 'вы успешно вошли');
	break;
	case ( name != arrName[i]):
	alert ( 'Ошибка' );
	break;
	case ( i != arrName.length-1):
	alert ( 'Ошибка' );
	break;
	default:
	alert ( 'Ошибка' );
}
